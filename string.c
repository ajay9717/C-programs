#include <conio.h>
#include <stdio.h>
#include <string.h>

void main()
{
    char str[100];
    int len;
    printf("\nEnter the String : ");
    scanf("%s", &str);
    len = strlen(str);
    printf("\nLength of Given String : %d", len);

    
}